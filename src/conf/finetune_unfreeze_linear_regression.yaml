inherit: 
    - exp_base.yaml

# pretrained weights
model:
    pretrained: True
    freeze_backbone: False

training:
    task: linear_regression
    curriculum:
        points:
            start: 11
            end: 41
            inc: 2
            interval: 2000

out_dir: ../models/finetune_unfreeze_linear_regression

wandb:
    name: "finetune_unfreeze_linear_regression_standard"
